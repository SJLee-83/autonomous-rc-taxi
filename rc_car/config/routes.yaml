# 강제 지정 번호 노선 (시연 시나리오) — 2026-08-06 오전 사용자 확정: 시나리오 2
#
# 시나리오 2: 4번 중앙 대기 → 면사무소(14) 픽업 → 우리집(20) 하차
#   (우리집 정차 차선은 2026-08-09 에 3번 → 20번 중앙으로 이동 — map/places.yaml home 참조)
# 이 두 노선은 플래너(길이 최단) 자연 선택과 **동일**하다 — 강제 지정은 결정론 고정
# + 배포 누락 감지(말뚝 테스트) 목적이다. 시나리오 1(안 1: 12→30→13→14→19→20→3)은
# 대체되어 제거 — 이력은 자료/시연시나리오.md.
#
# 번호 체계: 자료/차선번호맵_0806.html · navigation/lane_route.py NUM2ID
# 적용 규칙 (behavior/driving_worker.py _forced_route):
#   관제 호출의 목적 차선이 노선의 마지막 번호이고 현재 차선이 노선 위에 있으면
#   그 지점부터 이 노선을 그대로 탄다 (중간 재호출·오류 복귀 대응).
#   해당 없으면 기존 최단경로(Dijkstra §14) — 이 파일이 없어도 주행은 된다.
# 검증: 기동 시 전 노선 연결성 검사 + 기동 로그 "강제 노선 등록" 확인.
forced_routes:
  # 픽업 앞에 3을 붙인 이유 (2026-08-06 오후): 출발 배치가 이음매보다 서쪽(3번 차선)으로
  # 밀려도 동선이 강제되게. 4번 위 출발이면 부분 노선(4→…)이 적용돼 동작 동일.
  - [3, 4, 29, 7, 22, 14] # 시나리오 2 픽업: 3~4번 → 면사무소(14). 직1+우3+좌1
  # 🔴 [14,19,20,3] → [14,19,20] (2026-08-17 리팩터링 정정): 우리집 정차 차선이 20번으로
  #   옮겨진 뒤에도 종점이 3으로 남아 있었다. _forced_route 는 "목적 차선 = 노선 마지막
  #   번호"일 때만 매칭하므로 옛 노선은 매칭 실패 → 시연에서는 Dijkstra 폴백이 우연히
  #   같은 경로를 골라 무사했다. 이제 강제 노선이 의도대로 매칭된다.
  - [14, 19, 20]          # 시나리오 2 목적: 면사무소(14) → 우리집(20번 중앙). 회전 우2
