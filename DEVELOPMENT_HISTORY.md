# DEVELOPMENT_HISTORY (기능별 개발 이력, 구현 방식과 시행착오)

> 다태워 프로젝트 차량 임베디드 파트에서 **내가 수행한 작업**을 기능·일정 단위로 정리한 문서.
> 각 기능을 어떻게 구현했는지, 어떤 시행착오를 거쳤는지를 담음.
>
> 출처: 개발 기간 전체(2026-07-21\~08-10)의 작업 로그 §0-1\~§0-57, 트러블슈팅 기록 139항목,
> 통신 규약 문서(protocol_1/2), 회전 주행 개선 계획 문서.
> 팀원이 만든 것은 각 절과 §13에 명시했음.

최종 갱신: 2026-08-31

---

## 0. 한눈에 보는 타임라인

| 기간 | 단계 | 핵심 산출 |
|---|---|---|
| 7/21\~7/22 | 주제 확정·설계 착수 | 파트 분류 검토, 기술설계초안, 전원(PD → DC) 조사, 명세서 단일 기준 전환 |
| 7/23\~7/24 | **통신 규약 작성** | protocol v1.0 → v2.8 (검증 3회 누적 29건 결함 해소, 상태 기계 완결) |
| 7/24\~7/27 | **클라이언트 구현·검증** (Phase 1 전반) | 자체 관제 서버, rc_car 골격 + SafetySupervisor, 관제/GPS 클라이언트, motion_estimator |
| 7/28 | **맵·경로 계획** (Phase 1 완결) | 차선 그래프 맵 생성기, Dijkstra 경로 계획, 도착 판정, 통합 시뮬 S1\~S6 |
| 7/29\~7/30 | 팀 통합 | 정차점 확정, 웹팀 백엔드 교차 테스트, 실차 반입·T0\~T5 실측 |
| 7/31 | **첫 실물 자율주행 완주** | frame_transform 좌표 변환 계층, GPS 캘리브레이션 오류 발견 |
| 8/1\~8/2 | 사후 분석·개선 | 회전 개선 계획(시뮬 근거), SIGHUP 안전 보강, 도착 트리거 분리, 트러블슈팅 전수 문서화 |
| 8/3\~8/5 | 실차 주행 1\~3일차 | 트림 실측, 스로틀 개편, 이음매 개편, Vision 통합(vision_runner), 안전장치 3중 신설 |
| 8/6 | **주행 로직 전면 재설계** | 번호 노선 point-to-point + 비전 회전 트리거, 시나리오 2 무개입 완주 2회 |
| 8/7\~8/8 | 회전 = 하드웨어 문제 확정 | 직선 3대책, 회전반경 GPS 무관 실측(우 0.545 / 좌 0.650), 회전 개루프화, 연속 헤딩 추정기 |
| 8/8 심야\~8/9 | 우회 시도 | IMU 방위 스택 구현(미검증 폐기), 시간 기반 하드코딩 착수 → 철회 → 로그 역산 재개 |
| 8/10 | **시연 완료** | noah 폐루프 무개입 완주 3회 (하드코딩은 실차 0회로 폐기) |

---

## 1. 통신 규약 작성 (7/22\~7/24)

**무엇을.** GPS(실내 ArUco 측위 서버) · 관제 서버 · 차량 3자 간 WebSocket JSON 규약을 작성했음. 팀 초안(input.md)을 받아 v1.0으로 구조화한 뒤, 실측·검증을 거쳐 **v2.8 / 스키마 protocol_version 2.3** 으로 확정했음. 내부 상세판(protocol_1)과 팀 배포 정본(protocol_2, 1031 → 약 560줄 압축)으로 이원화했음.

**어떻게.**
- 골격: 모든 구간에서 차량이 client 임. 차량 → 관제는 `info`(5Hz 상시)와 `report`(accept · complete · error · recovered), 관제 → 차량은 `command`(move · stop · resume)
  - `stop` 은 정지 명령이 아님. 도착 확인과 상태 전이 신호임
  - `resume` 은 복귀 허가이며, 달리라는 지시가 아님
- 상태 기계: move 의 픽업/하차 구분은 좌표가 아닌 **직전 상태**가 결정함. state × command 수용 매트릭스, 오류 시 직전 state 보관 후 복귀. "모든 상태 × 모든 이벤트에 정의된 동작 존재"까지 밀어붙여 완결했음
- 검증 방법: 정상 시나리오 밖(기동 직후·재연결·응답 유실)까지 상태 기계를 시뮬레이션하는 자체 검증을 3회 반복했음
  - 1차 보완 12건(F1\~F12), 2차 9건(H1\~H9), 3차 4건(I1\~I4). 누적 29건 발견·전건 반영

**시행착오.**
- **실측이 문서를 뒤집었음**: 명세서 축간거리 0.075m 가 실측 0.14m 로 1.9배 오차였음(Pure Pursuit 회전반경을 절반으로 계산했을 값). 조향도 PWM 이 아닌 서보 각도였음(중립 108, 바퀴각 = (서보각 − 108) × 0.526). 안전 임계값을 전면 재계산했음
- 명세서 §23.3 이 팀 결정과 정반대("관제 유실 시 계속 주행")로 남아 있는 것을 발견·수정했고, 다음날 §29 에서 같은 내용의 잔재를 또 발견했음
  - 교훈: 한 곳을 고치면 전문 검색으로 잔재를 확인함
- F1(최중요): "관제 끊기면 즉시 정지" 규정에 **판정 수단이 없었음**(TCP keepalive 기본 2시간). WebSocket ping/pong(1s / 2s)으로 최대 3초 감지, 서버팀 구현 0줄로 해결했음
  - 교훈: 판정 수단 없는 안전 규정은 작동하지 않음
- F12: timestamp 정수화 규정대로면 10Hz pose 의 90%가 순서 판정에서 폐기되는 자기모순을 발견해 소수를 허용했음
- v2.7 에서 정한 것 2건(주행 중 stop 수락, timestamp 구간별 분리)을 v2.8 에서 스스로 철회했음. 결함이 경계 상황(기동·재연결·유실)에 몰린다는 것을 체득한 결과임

---

## 2. 웹 관제서버 ↔ 차량 통신 연결 (7/24\~8/3)

**무엇을.**
1. 웹팀을 기다리지 않기 위한 **자체 관제 서버**(control_server: 상태 추적·유실 주입·이벤트 로그)와 규약 완전 준수 가짜 차량(fake_vehicle.py, 이후 실차 코드의 레퍼런스)
2. 차량 측 관제 클라이언트 5모듈(control_client / command_policy / report_manager / telemetry / messages)
3. 웹팀 Java 백엔드와의 교차 검증
4. 시연 관제(Java)의 로컬 운용 체제

**어떻게.**
- 전용 스레드 안 asyncio 루프, ping 1.0 / 2.0, 재연결 1초. 상태 전이는 `command_policy` **단일 지점**으로 모았음
- 오류를 사유 집합 {control_lost, gps_lost}로 관리해 "모든 사유 해소 후에만 복귀"로 두었음. 동시 유실(§7.5)이 별도 코드 없이 자연 처리됨
- complete / error 는 1초 재전송에 timestamp 를 최초 발생 시각으로 고정했음. (report, timestamp)가 사건 식별자임
- 교차 검증: 7/30 웹팀 Java 백엔드 상대 14항목 전부 PASS(차량 config 무수정, `--control-url` 한 줄 교체) → 같은 날 밤 실차 무선 E2E 5시나리오(GPS 유실·관제 절단·동시 유실 포함) 전부 PASS
  - 규약 v2.3 을 각자 구현한 두 코드가 무수정으로 통했음. 규약 상호운용의 실증임
- 8/3 시연 관제를 내 노트북 운용으로 전환했음: JDK 설치·빌드, application.yml 실측값 반영, React 프론트 same-origin 빌드 내장, SPA 404 해결 컨트롤러와 주행 초기화 버튼 신설

**시행착오.**
- 기동 직후 최초 접속 실패는 오류로 격상하지 않음(유지되던 연결이 끊긴 경우만 §7.3). 경계 상황 구분을 설계로 반영했음
- 웹팀 CommandSender 의 좌표 변환 TODO 는 실행 시 **이중 보정**이 됨을 판정하고, frame_transform 항등 유지를 주석으로 명문화했음
- ssh 원격 백그라운드 기동 함정(`&&` + nohup 체인이 세션을 물고 안 끊김), 포터블 JDK 가 임시 폴더라 세션 후 소멸. 둘 다 절차 문서화로 대응했음
- **귀속**: Java 백엔드·React 프론트·규약 테스트 25건은 웹팀 제작임. 내 몫은 차량 클라이언트, 자체 관제 서버, 검증 스택, 로컬 운용 전환임

---

## 3. GPS 서버 ↔ 차량 통신 연결 (7/27\~7/31)

**무엇을.**
1. localization_client (wss 구독, 재연결 시 subscribe 재송신)
2. pose_validator (§3.4 전 규칙: 순서 역전·맵 범위·점프 검사 + 마커 오프셋 보정)
3. localization_service (유실 / 정상화 판정 단일 지점)
4. motion_estimator (info.speed 산출)
5. **frame_transform 좌표 변환 계층**과 적합 도구

**어떻게.**
- GPS 서버 원본 코드를 읽어 미문서 동작 4건(close 4404 / 4408, camera_frame_stale 등)을 파악하고, **원본을 로컬로 기동해 검증 상대로 사용**했음. 차량·마커·실지도 없이 전 경로를 PC 에서 검증했음
- **합성 카메라**(fake_camera.py): ArUco 마커를 그린 프레임을 서버에 push 해 정답을 아는 검증을 구성했음. heading 4방위 대조, 유실 0.61초 후 오류 정지 → 자체 복귀까지 규약 사슬을 재현했음
- ping/pong 대신 pose_timeout 무수신을 끊김으로 판정했음(절반 열린 연결 방어). `found=false` 는 **즉시** 출력 차단(stale), 0.6초 지속 시에만 ERROR
  - 규약 §3.4 와 §7.4 의 외견상 충돌을 "서로 다른 두 시점"으로 분리해 해소했음
- 속도 추정: 엔코더가 없어 pose 차분 + 이동평균 5 를 썼음. 시간 축은 수신 시각이 아닌 **촬영 시각**을 기준으로 해 네트워크 지터를 배제했고, 유실 시 속도를 0으로 강제했음. E2E 오차 0.18%
- 7/31 서버 좌표계와 맵 좌표계의 어긋남을 맵 재생성 대신 **pose 수신부 1곳의 아핀 변환**으로 해결했음. 맵 소비자(관제·정차점·웹팀) 전체를 흔들지 않는 선택이었음. 전용 프로브가 없어 차량 마커 큐브 자체를 프로브로 실측했음

**시행착오.**
- close 4404 / 4408 처리를 계획에 적어놓고 구현에서 빠뜨렸음. 설정 오류인데 로그 없이 무한 재접속했고 리뷰에서 발견했음. 조용한 실패의 전형임
- "실서버 접속 불가 = 망 밖"으로 오진했음. 실제는 서버 미기동이었고, ARP/TCP 증거로 판별하는 법을 배웠음
- 프레임 누락 뒤의 정상 이동이 점프로 오거부돼, 점프 허용치를 경과 시간에 비례하도록 바꿨음
- **우리 실측이 서버 캘리브레이션 오류를 잡아냈음**: 카메라 2대의 x축 32cm 불일치를 발견해 재캘리브레이션을 유도했음. 재배치 후 위치 sd 약 1cm, heading 노이즈 14.7° → 2.9°
- **귀속**: GPS 서버 자체·마커 배정·큐브 4면 중심 통합은 위치 서버팀임. 내 몫은 클라이언트·검증기·변환 계층·측정 도구, 그리고 서버 오류의 발견임

---

## 4. 차량 기본 제어 (7/23\~8/4)

**무엇을.** 팀 구동 코드(motor / servo controller, 팀원 작성)를 원본 무수정으로 통합하고, 하드웨어 실측으로 제어 파라미터를 확정했음.

**어떻게.**
- **원본 무수정 원칙**: `hardware/vendor/` 사본(동일성 테스트 감시) + config 브리지 + Real 어댑터(음수 throttle 거부, 바퀴각 → 서보각 변환). adafruit 의존성은 real 모드에서만 지연 로드해 PC 개발에 영향이 없음
- 실측 사다리 T0\~T5 를 무동력·안전한 것부터 설계했음: 보드 인벤토리 → 온보드 테스트 → **기동 관문**(pose 없으면 모터 미동, SafetySupervisor 실물 검증) → MOTOR_SIGN·서보 실측 → 무선 E2E → 속도·정지 거리
- 확정값: MOTOR_SIGN = -1, 서보 48 / 108 / 168(팀값 채택), 최고 속도 0.22m/s, 정지 거리 약 0(감속 기어 저항), 서보 중립 트림 117 → **118**(GPS 실측)

**시행착오.**
- **서보 클램프 함정**(코드 리뷰로 발견): 팀 코드의 `max(RIGHT, min(LEFT, angle))` 은 LEFT > RIGHT 수치를 전제함. 실측값을 이름대로 넣으면 조향이 항상 최대 우측으로 고정됨. vendor_bridge 가 전제 위반 시 기동을 거부하도록 차단했음
- 직진 드리프트: 서보 중앙인데 유효 바퀴각이 좌 4° 틀린 것을 실측으로 발견했음. 육안 판정 실패를 3회(7/30 · 8/4 두 차례) 겪고 "트림은 육안 금지, GPS 실측만"으로 원칙화했음
- mock 회귀 3회: 재배포가 보드의 `driver_mode: real` 을 PC 정본(mock)으로 덮어 모터가 무반응이었음. 모터 칩 레지스터 직독으로 소프트웨어 원인임을 확정했음. config 원복 누락이 반복 패턴임을 인정하고 배포 후 확인을 절차화했음
- 보드 시계 문제(RTC 없음 + 교육장 NTP 차단, 19.7일 지연): 관제 서버 HTTP Date 헤더로 부팅 시 자동 동기화하는 systemd 서비스로 종결했음
- 스로틀 개편 왕복: 감속 3종 신설 → 팀 결정으로 역전(직진 0.3 / 회전 0.5) → 회전 중 스로틀 강하 발견(lookahead 조기 전환) → 코너 출구 홀드 0.2m 추가

---

## 5. 맵 데이터 구축 · 경로 계획 (7/28\~8/4)

**무엇을.** 시연 지도 픽셀 판독(±2cm)으로 차선 그래프 맵을 구축하고, 그 위에 경로 계획과 도착 판정을 얹었음. 산출물은 맵 생성기(build_demo_map.py), 로더(lane_map.py), MapMatcher(§12 pose → 차선 매칭), DestinationResolver, RoutePlanner(Dijkstra), ArrivalChecker(도착 5조건), places.yaml(시연 6곳 정본)임.

**어떻게.**
- **좌표를 수기로 치지 않음**: 차선-커넥터 접점은 회전 호 접선 트림 구조라 수작업 시 연속성이 반드시 깨짐. 생성기가 트림·호 샘플링·연속성 검증까지 수행하고, 오버레이 그림으로 육안 검수함
- 맵의 구조적 성질(순환계 연결성)을 테스트로 고정했음. 맵 수정으로 연결이 깨지면 테스트가 먼저 잡음
- 정차점 검증: 도착 반경 ±0.10m 를 1cm 간격 21지점 × 6곳 전수 확인했음. 임의 좌표는 팀 Java resolver 를 재현해 768점 전수 검사했음

**시행착오 (이 영역의 백미 두 가지).**
- **두 순환계 발견**: 차선 변경 금지와 노면 화살표 규칙의 구조적 귀결로, 맵이 도달 불가능한 안쪽과 바깥쪽 두 순환계로 완전 분리됨을 발견했음. 설계 검토 2회를 거치며 수정 방향이 진화했음
  - 교차로 대각 통과(도로 문법 위반으로 폐기) → 점선 구간 차선 변경 커넥터 8개 + 회전 진입 차선 선택용 `*_far` 커넥터 16개
- **이음매 데드락 3연작**: 차선을 a/b 조각으로 나눈 대가로, 조각 경계(이음매) 근처 정차 시 도착 게이트가 영영 열리지 않는 교착이 3번 다른 얼굴로 나타났음
  1. 통합 시뮬 S6 이 커넥터 위 정지 케이스를 적발함(진출 차선 환산으로 수정, 반쪽 조치)
  2. 시연 정차점 3곳이 같은 함정 위에 있었음(좌표 이동으로 회피, 코드 보완은 보류로 명문화)
  3. 팀의 지도 클릭 임의 배차에서 재발함(768점 중 18.2% 데드락). 보류해 둔 보완안(진입·진출 양쪽 인정)을 적용해 768/768 통과했고, **팀원 코드는 0줄 수정**이었음
  - 교훈: 부분 수정이 대칭 케이스를 남김
- 8/4 이음매 개편: 짧은 이음매(0.5m)에서 차선 변경 기동이 물리적으로 무리(목표 5cm 초과·인도 침범)임이 실차에서 드러났음. 차선변경·keep 커넥터 16개를 폐지하고 a/b 8쌍을 병합했음(40 → 32차선). 시뮬에서 옳던 구조를 실차가 다시 고친 사례임
- 제자리 목적지 크래시(길이 0 경로 → 주행 워커 사망)를 사후 재검증에서 재현·수정했음. 같은 버그가 8/6 재설계 때 시뮬에서 또 잡혔음(회귀 테스트의 가치)

---

## 6. 안전 장치 (맵 데이터 × GPS 대조 포함, 7/24\~8/5 누적 진화)

**무엇을.** "위치를 모르면 정지"를 구조로 강제하는 다층 안전 장치임. 단번에 만든 것이 아니라 **사고가 한 층씩 쌓았음**.

- **SafetySupervisor**(7/24, 골격 단계): 모든 모터 출력의 단일 통과점임. pose 없음 / 비주행 상태 / 래치 시 무조건 정지함. 실차 첫 기동(T2)에서 "pose 없으면 모터 미동"으로 실물 검증했음
- **맵 × GPS 대조 계열**: pose_validator 의 맵 범위·점프 검사(물리 최대 이동의 4배 초과 거부), MapMatcher 역방향 출발 거부(§12.3, 실차에서 역주행 배치를 실제로 걸러냄), map_bounds 맵 경계 가드, 터널 모드(GPS 불량 구역 한정 추측항법, 한도 1.2m / 30s), 남동 위험 구역 판정과 경로 사전 검증 절차
- **종료·동결 계열**: SIGHUP / SIGTERM → SystemExit 핸들러(8/1, 7/31 폭주 분석에서 프로세스 즉사 시 PCA9685 가 마지막 PWM 을 래치함을 확인), 로깅 비블로킹화와 **독립 모터 가드**(guard.py, 별도 프로세스가 20Hz 하트비트를 감시하고 0.7초 정체 시 PCA9685 레지스터에 I2C 로 직접 FULL_OFF)

**시행착오.**
- 8/5 동결 사고가 설계 구멍을 정확히 찔렀음: WiFi 순단 → tee 파이프 역압 → 로깅 락 → **전 스레드(워치독 포함) 동결** → 모터 래치로 맵 밖 이탈. SIGHUP 핸들러는 종료만 커버하고 동결은 무방비였으므로, 프로세스 밖 독립 가드로 답을 냈음
  - 교훈: 안전 장치는 "내가 죽는 방식"의 목록만큼만 유효함
- 새 방어의 부작용이 실주행에서 즉시 드러난 사례 2건(8/7): 맵 경계 여유 0.08m 가 정상 융합 회전을 죽임 / 정지 조향 금지 설정이 주행 중 조향까지 얼려 95초 직진. 방어 추가에도 실측 검증이 필요함
- 도착 정지 트리거·게이트 분리(8/2): 정지 오차가 곧 반경 그 자체가 되는 겸직 구조를 규명하고, 트리거를 0.05m 로 분리했음(오차 예산 실측 기반 하한 5cm). 예고한 잔여 리스크(저속 데드존 미도달)가 8/3 실차에서 그대로 재현됐음. 리스크 명시의 가치임

---

## 7. Vision 연동 ① 계약 설계와 차선 보조(seg 조향 보정) (7/26\~8/6)

**무엇을.**
1. seg 인터페이스 계약서 작성·개정 (v0.1 → v0.3)
2. mock seg 모델과 어댑터
3. 실모델 실행체 vision_runner (별도 프로세스)
4. 융합 조향 (GPS 기저 + seg 보정)

**어떻게.**
- 계약 전략은 규약과 같은 방식임. **모델 완성 전에 경계 데이터부터 확정**해 병렬 개발했음
  - 출력은 3필드(valid / lateral_offset_m / heading_error_deg)로 두어, seg 와 GPS 대체 동작이 같은 형식을 갖게 했음. 조향 입력원 교체가 가능해짐
  - 실패 규약: 주행 중 예외 금지, `valid=false` 면 GPS 로 대체함. **모델 실패는 정지 사유가 아님**
- 구조 원칙: 주행의 주인은 GPS 이고, seg 는 ±8° 클램프 보정임(오검출 피해 상한). 보정 개입은 4중 게이트(직진 + 코너 밖 + 쌍 검출 + 정렬)를 통과해야 함
- vision_runner(8/5): CSI 캡처 → 버드아이 → 황색선 휴리스틱 → TensorRT 엔진(FP16, 8.8FPS) → `/dev/shm` 원자 게시. 차량 측은 논블로킹 `latest()` pull 임. 크래시·의존성 격리를 위해 별도 프로세스로 뒀음
- 보드 GPU 스택 수리: torch 사망의 진범이 pip 의 서버용(SBSA) CUDA 라이브러리가 Jetson 네이티브를 가로챈 것임을 규명했음(전날 진단은 오진이었음). 잔재 정리로 3.2GB 를 회수했음

**시행착오.**
- "자율주행을 Vision 파트로 이전하면?"(7/29) 검토에서 전면 영향 분석 후 유지를 권고했고 채택됐음. 3필드 계약도 합의 전인 상대에게 주행 전체를 얹을 수 없었고, **대체 동작 상실이 유일하게 못 되돌리는 손실**이었기 때문임
- YOLO 클래스는 조향에 바로 쓸 수 없음(7/29): 계약은 미터·도 단위인데 모델 출력은 이미지 좌표였고, 기하 계층(캘리브레이션·IPM)이 어디에도 없음을 짚어 raw mask + 임베디드 후처리로 방향을 정리했음
  - `dashed_line` 라벨이 실선까지 포함하는 "이름이 거짓말하는 인터페이스" 함정이 있었음. 차선 변경 판단은 비전이 아닌 맵 소관으로 못박았고, 이후 방침 번복 논쟁에서도 이 결론이 유지됐음
- Vision 파트 방침 변경 2회(클래스 5종 → 2종 + 색 검출)에도 **주행 로직 수정 0** 이었음. 3필드 어댑터 구조가 설계 의도대로 충격을 흡수했고, 계약서만 v0.2 → v0.3 으로 개정했음
- 융합 첫 주행 사고(8/5): 코너에서 시야가 돌아 heading 을 +26° 오인 → 풀클램프 보정 누수 → 도착 스침 → 배회. 녹화 프레임으로 증거를 확보한 뒤 게이트 3종(위치 기반 코너 창 차단 등)으로 봉합했음
- 실전에서 seg 차선 보조의 실사용률이 낮았음(트레이스 상 개입 0.3\~39%). 그 원인 분석이 아래 ②로 이어짐

---

## 8. Vision 연동 ② 회전 타이밍 결정(비전 트리거) (8/5\~8/6)

**무엇을.** 주행 알고리즘을 전면 재설계했음. 직선은 차선 번호 노선 point-to-point 이고, **회전 개시는 노면표시(방향표시·정지선·횡단보도)가 버드아이 하단에 진입하는 시점**임. GPS 는 "지금 11번, 다음 24번 우회전"까지의 거친 게이트만 담당함.

**어떻게.**
- 근거 데이터: 0805 트레이스 9,476프레임 분석에서 노면표시 검출률은 71\~83% 인데 기존 seg 차선 보정 사용률은 0.3\~39% 였음. 비전 활용도가 낮았던 원인이 모델이 아니라 쓰는 방식에 있었음
- 원거리 오검출(타 차선 표시 혼입) 대책: 버드아이 사다리꼴 하단은 좁아 자기 차선만 남으므로 **가장 인접(하단) 지점 트리거**로 뒀음. 트리거 기준을 미터가 아닌 픽셀 행 비율(near_row_frac)로 정의해 세로 캘리브레이션 없이 동작하게 했음
- 회전은 FOLLOW → ARMED(차선 끝 0.6m 이내) → TURNING(비전 작동) 상태기계임. 고정 조향각 atan(축거 / R), 종료 조건은 다음 차선 방위 정렬임. 비전 사망 시 좌표 단독 개시로 대체함(비전 0으로도 완주, 무중단 대체 동작 유지)
- 회전 48개를 180° 점대칭 16쌍으로 묶어 튜닝 클래스를 24종으로 반감했음(대칭 상속). 근거 없는 값은 표에 못 넣도록 말뚝 테스트가 강제함
- 결과: 8/6 현장에서 22 → 14 비전 트리거가 **7/7 작동**했고, 시나리오 2 무개입 완주 2회를 기록했음. 좌표계가 세 번 바뀌는 동안 매번 같은 지점에서 작동했음. 트리거의 가치는 정확도보다 **좌표계 독립성**에 있었음

**시행착오.**
- 모든 커넥터를 하나의 피드포워드 공식으로 처리한 것이 반복 실패의 뿌리였다는 규명이 재설계의 출발점이었음
- 세로 캘리브레이션 역산 2회 실패했음. 근본 원인은 정상 주행 중 pose + heading 로그가 아예 없었던 것임(WARNING 줄에만 존재)
  - 교훈: 로그는 남기는 것이 아니라 설계하는 것임
- 수동 주행 데이터로 트리거 값 역산을 시도했음(8/7). 사람의 조향 시점과 30\~50cm 어긋나 전량 롤백했고, 어긋난 이유는 끝내 미규명임
- 격리 방식 논쟁: 구 로직을 config 스위치로 두자는 내 제안이 기각됐음. mock 회귀가 재배포로 3번 되살아난 전례와 같은 길이었기 때문임. 환경변수 단독 게이트와 말뚝 테스트로 격리했음

---

## 9. 주행 제어 개선 사가 (직선과 회전, 7/31\~8/10)

이 프로젝트에서 가장 긴 원인 규명 사슬임. 요약하면 **관찰 → 시뮬 재현 → 파라미터 개선 → 실차 반증 → 하드웨어 문제 확정 → 실측으로 흡수 → 상류(측위)에서 해결** 임.

**직선.**
- 7/31 차선 침범을 원인 분해했음: 좌 4° 틀림 3.5cm + GPS 중심 밀림 3\~4cm 가 여유 5cm 를 넘었음
- 8/7 직선 3대책이 전부 실측으로 효과를 확인했음
  1. heading 가드: GPS 방위가 위치 기반 진행방향과 25° 이상 벌어지면 대체함. 18:04 에 GPS 방위가 +80° 틀린 것을 믿고 풀락으로 꺾은 사고가 근거임
  2. 끝점 겨냥을 중심선 투영 + lookahead 정석 추종으로 교체했음. 사고 3건이 같은 구조임을 로그로 규명했음
  3. 교차로 직진 게이트와 데드밴드 추가
  - 최대 오차 0.398 → 0.112m
- 8/8 **연속 헤딩 추정기**: heading 가드의 이진 전환이 30초에 19회 토글하며 0.37m 진동을 만드는 것을 확인하고, 전환 대신 연속 혼합으로 교체했음(자전거 모델 + 위치 코스각 α=0.45 + 마커 heading 약보정 α=0.03). 타 프로젝트(HY) 인계 문서의 방식을 이식했음
  - 회전 정렬 오차 158\~174° → 7.7\~11.9°

**회전.**
- 7/31 회전 반경 과대 → 8/1 시뮬(turn_param_sim, 실제 차량 코드 + 자전거 모델 폐루프) 신설. 원인은 조향 이득 부족이었음(코너 필요각 22°가 오차 33°에서야 출력됨) → 8/2 이득 45 → 30, 커넥터 속도 0.12 → 0.20, 정렬 감속 면제를 적용했음(시뮬 예측 이탈 −42%)
- 8/5 피드포워드 위상 결함을 시뮬이 실차 전에 적발했음(FF ON 이 인도로 12.2cm 이탈). 곡률 기반으로 수정했고 시뮬 4.1cm 대 실차 3.5cm 로 정합했음. 그래도 좌회전이 늦어 커넥터 기하 자체 문제로 판정했음
- 8/6 서보 우측 한계 발견(클램프와 트림으로 우회전 필요각이 물리적으로 안 나옴) → 8/7 "조향이 지령의 40%만 먹는다" 확정. 스로틀 가설은 실측으로 기각됐음(코너 초과가 정확히 2배로 정반대였음)
- 8/8 **회전반경 GPS 무관 실측**: 시간 기반 원 주행 러너(t6)로 풀락 반원 지름을 줄자로 실측했음. 우 R 0.545 / 좌 0.650, 5회 재현했음
  - 핵심 통찰: 틀렸지만 재현성이 있으면 계산으로 흡수할 수 있음
  - lead = R실측 − R설계 로 공식화하고, 호 길이 min = max 로 고정해 **회전을 GPS 방위와 무관한 개루프**로 전환했음
  - 검증: 모델 예측과 전날 실측이 1mm 일치했음
- 8/9 서보 클램프 168 이 벤더 명세(165)를 초과했음을 발견·수정했음. 그 대가로 실효 반경이 0.545 → 0.61 이 됐고 회전표를 전부 재계산했음(기존 완주 로그의 궤적을 원 최소제곱 적합해 새 주행 없이 재측정)
- 8/10 시연: noah 폐루프 스택으로 무개입 완주 3회, 도착 오차 8.7\~19.3cm(회를 거듭할수록 개선)

**하드웨어 사고 계열 (병행).**
- I2C Errno 121 4연속: 가설(서보 스톨 전류 → 5V 강하 → PCA9685 브라운아웃 리셋)을 수립하고, 0x70 All-Call 출현을 전원 리셋 물증으로 확정했음
  - 복구 방식은 재시도가 아닌 **재초기화**로 정했음. 리셋 시 PWM 주파수 설정이 소실되므로, 재시도가 성공해도 주파수가 틀려 멈추는 것보다 나쁨. 보류하던 재시도 패치를 기각으로 격상했음
- 정지 상태 풀락 조향이 젯슨 보드 전체를 다운시키는 것을 현장에서 확인했음(주행 중 조향은 무사고). "정지 조향만 위험"으로 확정하고 물리 예방 3종을 뒀음

---

## 10. 폐기된 시도 2종 (우회로가 남긴 것)

### IMU 방위 스택 (8/8 심야 구현, 미검증 폐기)

- GPS 방위 180° 뒤집힘의 대안으로, 팀원이 장착·보정한 MPU6500 으로 방위 출처 자체를 교체했음: raw I2C 드라이버 + 100Hz 자이로 적분 워커(시드 + 바이어스 보정 + 정지 재보정 + GPS 대체). 보드에서 센서 검증까지 마쳤음(바이어스 std 0.04, 정지 드리프트 분당 0.4°)
- 폐기 이유: 자이로 부호·스케일·주행 영향이 전부 미검증인 채 시연 D-1 이 됐음. 부호가 틀리면 우회전을 좌회전으로 적분하는데 센서는 정상값을 주므로 대체 동작이 걸리지 않음. GPS 뒤집힘보다 나쁜 실패 모드라는 판단이었음
- 배운 것: 보정 JSON 을 실물 대조해 "센서가 뒤집혀 장착됨"(gyro_sign 반전 필요)을 문서보다 먼저 잡아냈음. 관측 공백을 부재로 읽지 말 것(§0-52 의 기록 정정 2건)
- 부수 사고: 밤 패키지 미커밋 상태에서 tar 전체 배포가 보드를 하루 전으로 되돌렸음. "배포 전 보드 현행 diff 확인" 절차의 기원임

### 시간 기반 하드코딩 스택 (8/9\~8/10 구현, 실차 0회 폐기)

- 시연 D-1, 하드웨어 리스크(모터햇 다운)를 안고 폐루프를 못 믿는 상황에서 시나리오 전 구간을 (서보각, 스로틀, 초) 세그먼트로 하드코딩하는 보험이었음. 1차 착수(8/9 밤)는 측정 자체가 보드를 죽여 좌초했음
  - 발상 전환: 값은 이미 완주 로그 안에 있었음. 로그 역산 도구로 재개했음(8/10)
- 산출: 스로틀별 직진 속도 분리(0.40 → 0.089 / 0.50 → 0.110 m/s), 회전 착지 실패의 원인 분해(7 → 22 는 기하 부족으로 성공 3/13 이 전부 90° 초과로 채운 것 / 14 → 19 는 개시 위치가 결과를 결정), 5축 변형 테이블과 프리셋 15종, 판정 철학(pass/fail 이 아닌 착지 좌표로)
- **역주행 값 적발**: 회전 시간 11.58초의 출처 로그를 전수 추출하니 반대 차선 침범(역주행) 착지 주행이었음. 오염된 값을 담은 프리셋을 폐기했음
  - 교훈: 근거 없이 채운 값이 근거 있는 값과 같은 표에 섞이면 안 됨. 출처(실측 / 기하 / 추정) 주석을 의무화했음
- 시연일 하드코딩 2회 실패("하드코딩은 위치를 보지 않음. 배치가 곧 전제") 후 폐루프로 전환했고, 폐루프가 완주하면서 실차 0회로 접었음. 다만 이 과정의 로그 역산 산출물(속도 테이블·착지 분석)은 폐루프 튜닝에도 쓰인 자산임

---

## 11. 시연 (8/10) 결과와 그 직전의 반전

- 기동 7회: 하드코딩 2회 실패 → noah 스택 병합·부분 위임 시도 → **폐루프 전면 복귀** → 무개입 완주 3회, 관제 왕복(complete ↔ stop) 6회 전건 성공
- 07:31 발견이 상징적임: 8/9 에 계산해 둔 회전 lead 수정값이 하드코딩 표에만 들어가고 폐루프 표에는 누락돼 있었음. 두 스택 병행의 동기화 비용을 시연 아침에 치렀음
- 최종 계보: 시연 실사용 코드는 `_보관\noah`(noah2 와 완전 동일), 포트폴리오 정본은 `제출_0810\rc_car`(noah + 보안 처리)임. 하드코딩 5축 스택 19파일과 IMU 스택 5파일은 폐기 구획임

---

## 12. 개발 인프라·검증 체계 (기간 전체 관통)

- **테스트**: 최종 프로덕션 약 26,700줄 + 테스트 약 11,500줄(439건). 값 고정이 아닌 **불변식** 테스트임(속도를 올리면 테스트가 먼저 잡는 구조). 말뚝 테스트로 폐기 로직 격리·대칭 상속·설정 회귀를 감시함. "테스트가 본 코드 로직을 복제하면 감시가 무력화된다"는 교훈으로 같은 함수 호출로 교체했음
- **통합 시뮬**: GPS 서버 원본 + 합성 카메라 + 관제 서버 + 차량 전 스택 4프로세스 폐루프임. 차량 코드는 자신이 시뮬인 것을 모름. 시나리오 S1\~S6 이 실버그(교차로 틈새 데드락, 길이 0 경로)를 실차 전에 적발했음
- **배포 규율**(사고가 만든 절차): 정본은 PC + git, 보드는 배포 대상 / md5 + compileall + 실행 검증 3중 / 배포 후 재기동 확인(config 는 기동 시 1회만 읽음) / 배포 전 보드 diff 확인 / tee 와 /tmp 금지
- **문서·팀 운영**: 폴더 재편 2회(전부 git mv 로 히스토리 유지), 팀 gitlab 업로드(MR), Jira 백필 시트, 트러블슈팅 전수 문서화(139항목, 정방향 정확성 + 역방향 완전성 2중 재검증, 위조·허위 0건 확인)

---

## 13. 귀속 정리 (포트폴리오 기재 기준)

> **git 히스토리 교차 검증 (2026-08-17, 개인 저장소 기준). 아래 표와 정합 확인했음.**
> - **자작 모듈은 점진 유입**: control_client.py 7/27(Phase 1-2 커밋), lane_map.py 7/28(D1 커밋), lane_follower.py 8/6(알고리즘 교체 커밋), heading_estimator.py 8/9. 각각 기능 설명이 담긴 개발 커밋으로 등장함
> - **팀원 수신물은 일괄 유입**: 관제서버(Java + React) 184개 파일이 8/3 단일 커밋 / taxi · motor · servo controller 원본 7/24 일괄(같은 커밋에 내 vendor 사본과 vendor_bridge 동반, "수신 즉시 리뷰·통합" 기록과 일치) / Vision handoff 패키지 8/2 · 8/4 일괄 / GPS 서버 소스는 아예 repo 밖(`Desktop\GPS\`)에 보관해 내 저장소에 커밋조차 안 했음
> - **부수 정합**: 8/8 날짜 커밋이 0건임. §0-51 "밤 패키지 미커밋 상태에서 배포 사고" 기록과 일치함
> - **팀 repo author 확정 검증** (2026-08-17, github.com/SJLee-83/SSAFY_PJT, 팀 GitLab 사본으로 히스토리 보존 확인):
>   - **차량 코드(orin-car/) = 본인 단독**: SJLee-83 author 커밋 2건(8/3 초기 업로드 MR d159f3e, 8/10 시연 완주본 + vision_runner MR 97a7f2c)이 실체 전부임. 그 외 커밋 1건은 `.gitkeep` 빈 폴더 스캐폴드뿐임
>   - **Java 백엔드(src/) · React 프론트(server/frontend/) · fake-vehicle = 웹 담당 팀원** / **GPS 서버(gps/) = 위치 서버 담당 팀원 단독**
>   - **팀장의 담당 역할은 git · Jira 관리임**. 팀장 커밋 12건(전체 최다)은 merge · 초기 구조 · 규약 업로드 등 관리성 커밋이며 실작성 귀속의 근거가 아님(8/17 확인). gps/ 경로의 팀장 커밋 1건도 관리성 업로드로 볼 것
>   - 유의: 팀 repo 의 `contracts/protocol.md`(v2.3)는 팀장이 관리 차원에서 커밋한 것임. 규약 원문(protocol_1/2, v1.0 → v2.8) 작성 이력은 개인 저장소 7/23\~24 커밋에 있으므로, 포트폴리오에서 규약 작성 기여의 근거는 개인 저장소 이력으로 삼을 것

**내가 만든 것 (차량 임베디드 전체)**

- 통신 규약 v2.6\~v2.8 문서화와 결함 도출 29건, seg 인터페이스 계약 v0.1\~v0.3
- rc_car 전 모듈: 관제/GPS 클라이언트, 상태 기계, 안전 계층(SafetySupervisor · 모터 가드 · 시그널 핸들러), 맵 생성기 · 경로 계획 · 도착 판정, 주행 제어(P 제어 → 번호 노선 + 비전 트리거 + 개루프 회전), 연속 헤딩 추정기(HY 프로젝트 방식 이식), vision_runner, frame_transform 계층
- 자체 관제 서버(control_server), 통합 시뮬 스택(합성 카메라 포함), 측정 도구 전체(t5 · t6 · t7 · 로그 역산 · 시뮬), 테스트 스택
- 실측·진단: T0\~T5, 회전반경 원 실측, 트림 GPS 실측, GPS 서버 캘리브레이션 오류 발견, I2C 브라운아웃 원인 규명

**팀원·타 파트가 만든 것 (내 것으로 기재 금지)**

- 웹팀: Java 관제 백엔드와 React 프론트 전체(테스트 254건 포함), CurbStopPointResolver, stop 유실 대응 수정. 나는 로컬 운용 전환 · yml 수정 · SPA 컨트롤러 · 초기화 버튼만 추가했음
- 위치 서버팀: GPS 서버 전체 소스 · 마커 배정 · 큐브 4면 중심 통합 · 카메라 캘리브레이션(물리 구축 포함)
- Vision 파트: YOLO seg 모델(best.pt) · birdseye 캘리브레이션 패키지 · 검출 방침. 나는 계약 · 어댑터 · 실행체(vision_runner) · 엔진 변환을 담당했음
- 팀 공통: motor / servo controller 원본, taxi/ 프로토타입, IMU 장착·보정 자산, 하드웨어 조립, 통신 규약 원초안(input.md), 보드 사전 세팅
- 외부: 연속 헤딩 추정기의 원 설계는 HY 프로젝트 인계 문서임(이식·검증은 본인)

**주요 설계 결정의 출처**

- 본인 결정: 명세서 단일 기준 전환, 관제 서버 자체 구축 선행, 주행 아키텍처(GPS 전역 + seg 국소 + 대체 동작 3층), 차선 변경 커넥터 방식, Vision 이전 안 함, 하드코딩 전환·철회, lead 확정값 등
- 팀 결정: 시연 = seg 조향 방향(초기), 스로틀 역전, 기어비 변경, 하드웨어 확정

---

## 14. 반복 패턴 (트러블슈팅 문서가 뽑은 8가지)

1. cp949 print 크래시 3회 (비ASCII 특수문자)
2. config 직접 수정 원복 누락으로 mock 회귀 3회
3. 이음매 데드락 3연작. 부분 수정이 대칭 케이스를 남김
4. 문서 잔재. 여러 곳 중 한 곳만 수정함
5. 조용한 실패가 가장 위험함 (무한 재접속, skip 테스트)
6. 요약·합성을 믿다 2회 피해. 원문이 우선임
7. 결함은 경계 상황(기동·재연결·유실)에 몰림
8. 실측이 문서를 이김

여기에 워크로그 후반이 보탠 것:

9. 트림·정렬은 육안 금지, GPS 실측만 (3회 실패)
10. 로그는 남기는 것이 아니라 설계하는 것 (3계열 소실: tee 덮어쓰기 · /tmp 초기화 · 인메모리 서버)
11. 틀렸지만 재현성이 있으면 계산으로 흡수 가능함
12. 근거 없는 값과 근거 있는 값을 같은 표에 섞지 말 것 (출처 주석 의무화)
